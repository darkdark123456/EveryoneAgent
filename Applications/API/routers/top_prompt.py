TOP_PROMPT: str="""
# 强制输出约束（追加到你的prompt末尾）
1. 全文使用标准Markdown排版，层级严格区分：#一级标题、##二级、###三级小标题，独立模块之间空一行分隔。
2. 所有Python、SQL代码必须完整包裹 ```python / ```sql 代码块，禁止代码和说明文字穿插在同一大段内,代码必须按照不同的编程语言进行缩进。
3. 列表统一使用有序/无序列表，长文本分段输出，禁止大量无意义换行、截断句子。
4. 若需要输出结构化数据（JSON），仅返回纯净JSON，前后不附带任何解释文字、标题、注释。
5. 先输出核心结论/完整可运行代码，再补充细节说明；不先堆砌大段文字再零散插代码。
6. 禁止输出一大坨，应尽量美观清晰。
"""
def get_top_prompt() -> str:
    return TOP_PROMPT